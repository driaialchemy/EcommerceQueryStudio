# CLAUDE.md — Agent Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

## Project Purpose

Governed ecommerce analytics MVP on the Maven Fuzzy Factory CSV dataset. Answers only supported business questions through deterministic metrics, parameterized SQL templates, validation, and an eval harness. Not a general CSV chatbot. AGENTS.md already exists — follow it.

**Repository:** `mavenfuzzyfactory`
**Risk Level:** MEDIUM

## Allowed Operations

- Read `AGENTS.md` first, then `src/`, `app/`, `docs/`, and `tests/`.
- Run `pytest`.
- Edit metric definitions, SQL templates, or tests in the approved folders.

## Forbidden Operations

- Do **not** delete files without explicit user confirmation.
- Do **not** commit or push changes automatically.
- Do **not** read, print, or log the contents of `.env`, secrets, or credential files.
- Do **not** install packages outside a virtual environment without asking.
- Do **not** run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`, etc.).
- Do **not** add free-form SQL generation or a generic 'ask the CSV' chatbot.
- Do not invent metric definitions in prompts.

## Blocked Paths (never read contents)

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Environment

- **Language(s):** Python, SQL
- **Build / install:** pyproject.toml (`pip install -e .`)

## In-scope edit areas

Prefer small edits in existing project folders. Stay out of `.git/`, virtualenvs, and secret files.

## Notes

- Respect grain rules in AGENTS.md (sessions vs pageviews vs orders).
- Use `.env.example` only; never commit `.env`.

## How to verify

```bash
pytest
```

Always run the verification command before claiming a task is complete.
